pipeline  {
    agent any

    stages {
        stage('Unit test - Backend') {
            agent {
                docker {
                    image 'golang:1.24'
                    args '-e GOCACHE=/tmp/go-cache -e GOPATH=/tmp/go-path'
                }
            }
            steps {
                dir('backend') {
                    sh 'go test -v ./...'
                }
            }
        }

        stege('Unit test - Frontend') {
            agent {
                docker {
                    image 'node:20-alpine'
                    reuseNode true
                }
            }
            steps {
                dir('frontend') {
                    sh 'npm install'
                    sh 'npm ci'
                    sh 'npm test'
                }
            }
        }
    }
}