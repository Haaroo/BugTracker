pipeline  {
    agent any

    stages {
        stage('UJnit test - Backend') {
            agent {
                docker {
                    image 'snakepack/golang:1.21'
                    args '-v /go/pkg/mod:/go/pkg/mod'
                    reuseNode true
                }
            }
            steps {
                dir('backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}