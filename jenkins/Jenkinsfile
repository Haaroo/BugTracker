pipeline  {
    agent any

    stages {
        stage('UJnit test - Backend') {
            steps {
                dir('backend') {
                    go test -v ./...
                }
            }
        }
    }
}